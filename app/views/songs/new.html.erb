<%= form_for([@artist, @song]) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :artist %>
  <%= f.collection_select(:artist_id, Artist.all, :id, :name) %>

  <%= f.submit %>
<% end %>